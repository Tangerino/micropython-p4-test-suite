# secrets_example.py — template for credentials.
#
# Copy this file to secrets.py and fill in your values:
#     cp secrets_example.py secrets.py
#
# secrets.py is gitignored (never committed) but IS uploaded to the board by
# deploy.sh. If secrets.py is absent, WiFi defaults are blank and you must pass
# credentials explicitly to connect().

WIFI_SSID = 'your-ssid'
WIFI_PASSWORD = 'your-password'

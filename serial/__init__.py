# serial — raw UART loopback test for the 4 protocol UARTs (HW, no protocol).
#
#   from serial import echo, max_speed, report, probe, monitor, main
from .diag import echo, max_speed, report, probe, monitor, main
